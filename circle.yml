machine:
  node:
    version: 5.5.0
dependencies:
  post:
    - npm run build
deployment:
  release:
    tag: /release-.*/
    commands:
      - chmod +x ./scripts/release.sh
      - ./scripts/release.sh
