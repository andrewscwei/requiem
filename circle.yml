machine:
  environment:
    USER_NAME: andrewscwei
    USER_EMAIL: andrew@variante.io
  node:
    version: 5.6.0
deployment:
  release:
    tag: /release-.*/
    owner: VARIANTE
    commands:
      - chmod +x ./scripts/release.sh
      - ./scripts/release.sh
